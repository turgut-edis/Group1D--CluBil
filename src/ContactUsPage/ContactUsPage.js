import React from 'react';

import { userService, authenticationService } from '../_services/authentication.service';

export default class ContactUsPage extends React.Component {

    render() {
        return (
            <div>
                <h1>ContactUsPage </h1>
            </div>
        );
    }
}
